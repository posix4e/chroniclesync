{
  "name": "sync-pages",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev src",
    "deploy": "wrangler pages deploy src"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}