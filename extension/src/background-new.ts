/**
 * Main background script entry point
 * This file is kept minimal and delegates to specialized modules
 */
import { initializeBackgroundScript } from './background/initialization';

// Initialize the background script
initializeBackgroundScript();