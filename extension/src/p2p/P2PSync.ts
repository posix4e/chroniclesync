/**
 * Mock P2PSync class for testing
 * This is a placeholder since we're using direct P2P connections without a discovery server
 */
export class P2PSync {
  constructor(
    _clientId: string,
    _mnemonic: string,
    _iceServers?: RTCIceServer[]
  ) {
    // Mock implementation
  }
}