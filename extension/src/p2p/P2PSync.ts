/**
 * Mock P2PSync class for testing
 * This is a placeholder since we're removing custom discovery support
 */
export class P2PSync {
  constructor(
    clientId: string,
    discoveryServer: string,
    mnemonic: string,
    iceServers?: RTCIceServer[]
  ) {
    // Mock implementation
  }
}